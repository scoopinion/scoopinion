- if admin? && current_user.id == 1
  = form_for ArticleTag.new, :remote => true do |f|
    = hidden_field_tag :article_id, article.id
    = text_field_tag :tag, "", :placeholder => "New tag..."
    = image_tag "loading-blue.gif", :class => "loading", :align => "top"